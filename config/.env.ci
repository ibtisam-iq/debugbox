# ============================================================
# DebugBox — CI Environment Configuration (.env.ci)
# Loaded by GitHub Actions using ci-env-loader.yml
# Contains NO secrets.
# ============================================================

# Core Project Info
PROJECT_ORG=ibtisam-iq
IMAGE_NAME=debugbox

# Registry Locations
REGISTRY_GHCR=ghcr.io/ibtisam-iq/debugbox
REGISTRY_DOCKERHUB=mibtisam/debugbox
REGISTRY=ghcr.io/ibtisam-iq/debugbox

# Variants built by CI
VARIANTS=lite balanced power

# Tool Versions
YQ_VERSION=v4.44.3

# YQ SHA256 checksum (value loaded via GitHub Secrets)
YQ_SHA256=""

# Build Cache (GHCR-backed)
BUILDX_CACHE_REF=ghcr.io/ibtisam-iq/debugbox:buildcache
