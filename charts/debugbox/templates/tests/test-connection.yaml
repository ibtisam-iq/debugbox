apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "debugbox.fullname" . }}-test-connection"
  labels:
    {{- include "debugbox.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "debugbox.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
